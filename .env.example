# ===========================================
# TVS Environment Configuration
# ===========================================

# Server
PORT=3000
NODE_ENV=development

# Database
DATABASE_URL=postgres://tvs:tvs_dev_password@localhost:5432/tvs
DB_PASSWORD=tvs_dev_password

# Feature Flags
USE_DATABASE=false      # Set to 'true' to use PostgreSQL instead of in-memory
USE_VEILFORMS=false     # Set to 'true' to use VeilForms API
USE_BITCOIN_ANCHORING=false  # Set to 'true' to anchor Merkle roots to Bitcoin

# VeilForms Integration
VEILFORMS_API_URL=https://api.veilforms.com
VEILFORMS_API_KEY=your_veilforms_api_key

# Encryption
# Generate with: openssl rand -hex 32
MASTER_KEY=replace_with_32_byte_hex_key_for_production

# ===========================================
# Bitcoin Anchoring (via OpenTimestamps)
# ===========================================
# OpenTimestamps provides FREE Bitcoin timestamping!
# No Bitcoin node required - works from anywhere.
# See docs/bitcoin-anchoring-setup.md for details.

BITCOIN_NETWORK=mainnet        # 'mainnet' or 'testnet' (for explorer URLs)

# ===========================================
# Production Settings
# ===========================================
# For production, ensure:
# 1. Strong DB_PASSWORD
# 2. Strong MASTER_KEY
# 3. USE_DATABASE=true
# 4. USE_BITCOIN_ANCHORING=true
# 5. NODE_ENV=production
# 6. Valid SSL certificates
